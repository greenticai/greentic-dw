# greentic-dw-operala-bridge

NATS event bridge for the Greentic deep-worker runtime: consumes greentic.operala.request.v1 and dispatches to OperalaDispatchInvoker.

This crate is part of the `greentic-dw` workspace.
